import { TransformToMultiselectPipe } from './transform-to-multiselect.pipe';

describe('TransformToMultiselectPipe', () => {
  it('create an instance', () => {
    const pipe = new TransformToMultiselectPipe();
    expect(pipe).toBeTruthy();
  });
});
