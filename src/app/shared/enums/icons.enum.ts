const path = 'assets/img/home/icons/';
const shareModalPath = 'assets/img/home/Donation/';

export enum Homepage {
  flagIcon = `${path}flag.svg`,
  arrowsTape = `${path}arrows_tape.svg`
}

export enum ShareModal {
  dialogImage = `${shareModalPath}dialog-image.png`,
  facebook = `${shareModalPath}facebook.svg`,
  instagram = `${shareModalPath}instagram.svg`,
  telegram = `${shareModalPath}telegram.svg`,
  copy = `${shareModalPath}share-copy.svg`,
  close = `${shareModalPath}close.svg`
}
