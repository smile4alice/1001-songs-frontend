export enum mapFilter {
  country = 'map.filter.categories.country',
  region = 'map.filter.categories.region',
  settlement = 'map.filter.categories.settlement',
  genre = 'map.filter.categories.genre',
  title = 'map.filter.categories.title',
  found = 'map.filter.categories.found'
}
